version: '3.4'

services:

  rmarket:
    build: .
    command: npm run start
    
    volumes:
      - .:/usr/app
      - /usr/app/node_modules
    expose:
      - 5555
    ports:
      - "5555:5555"
    
    restart: unless-stopped
